services:
  nhbackend:
    container_name: nh_backend
    build:
      context: ./backend/nhdream
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - nh_network

networks:
  nh_network:
    driver: bridge
